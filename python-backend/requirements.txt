# Rivet AI service — what ai_backend.py actually imports.
#
# This file previously held a 100+ package pip freeze (google-adk, alembic,
# opentelemetry, arq, …) covering three backend generations that were built
# during the rebuild and never wired up. Only ai_backend.py runs; see
# docs/CODEBASE_MAP.md. The old freeze is in git history if you ever need it.
#
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#   uvicorn ai_backend:app --reload --port 8000

fastapi==0.118.3
uvicorn[standard]==0.38.0
pydantic==2.12.5
python-dotenv==1.0.1
# 2.16.0 is the floor: 2.10.0 pins httpx<0.28, which cannot coexist with
# google-genai's httpx>=0.28.1, and versions below 2.16.0 reject the current
# sb_publishable_/sb_secret_ key format as "Invalid API key".
supabase==2.16.0

# Unified Google GenAI SDK — targets AI Studio (API key) or Vertex AI (ADC).
# Imported lazily: the service starts and serves mock quotes without a key.
google-genai==1.53.0
